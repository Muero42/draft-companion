name: HISTORICAL ARCHIVE - rc463-mock-live-parity.yml
on:
  workflow_dispatch:
permissions:
  contents: read
jobs:
  archived:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Historical workflow retained in docs/archive; never execute as current runtime.'
