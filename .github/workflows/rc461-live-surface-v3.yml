name: HISTORICAL ARCHIVE - rc461-live-surface-v3.yml
on:
  workflow_dispatch:
permissions:
  contents: read
jobs:
  archived:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Historical workflow retained in docs/archive; never execute as current runtime.'
