name: Expert-v2 WR-only exact gate
on:
  push:
    branches: [pitti-expert-v2-wr-only-20260826]
  workflow_dispatch:
permissions:
  contents: read
jobs:
  exact-return:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Exact Return-v2 control parity and WR-only treatment
        run: node research/wr-v2-exact-return-harness.mjs
