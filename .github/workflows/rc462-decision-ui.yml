name: HISTORICAL ARCHIVE - rc462-decision-ui.yml
on:
  workflow_dispatch:
permissions:
  contents: read
jobs:
  archived:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Historical workflow retained in docs/archive; never execute as current runtime.'
