name: legacy rc4.61 package (manual only)
on:
  workflow_dispatch:
permissions:
  contents: read
jobs:
  legacy-note:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "rc4.61 packaging is historical only."
          echo "Current successor packaging authority is release-contract-v2-package.yml."
          echo "Do not publish or infer current runtime state from rc4.61."
