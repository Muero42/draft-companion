import fs from 'node:fs';import assert from 'node:assert/strict';
const app=fs.readFileSync('app.js','utf8');
assert(app.includes('Start/Sit v6'));
assert(app.includes('globale legale Slot-Optimierung'));
assert(app.includes('START/SIT TEILWEISE NICHT BEWERTBAR'));
assert(app.includes('LINEUP OPTIMAL'));
assert(app.includes("rank.status==='VERIFIED'&&projection.status==='VERIFIED'"),'each candidate requires rank and projection');
assert(app.includes("!['RESERVE','IR','TAXI'].includes"),'inactive players excluded');
assert(app.includes("Number(m.delta)>0?'START':'REASSIGN'"));
console.log('SEASON_LINEUP_EVIDENCE_STATUS_PASS');
