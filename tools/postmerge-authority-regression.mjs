import assert from 'node:assert/strict';import {loadAuthority,validateAuthority,validateContinuationEvidence} from './postmerge-authority-contract.mjs';
const b=loadAuthority();assert.deepEqual(validateAuthority(b),[],'baseline v255 authority must validate');
const cases=[['generation',d=>d['PITTI_CURRENT_STATE.json'].handoff_generation='old'],['source',d=>d['PITTI_CURRENT_STATE.json'].authority.source_candidate='v11.8.0-rc4.204'],['merge',d=>d['PITTI_CURRENT_STATE.json'].authority.rc4205_source_merge.merge_commit='bad'],['fallback primary',d=>d['PITTI_CURRENT_STATE.json'].authority.rc4205_source_merge.repair_semantics='omit ros'],['false acceptance',d=>d['PITTI_CURRENT_STATE.json'].authority.rc4205_source_merge.device_acceptance_proven=true],['physical version',d=>d['PITTI_CURRENT_STATE.json'].runtime.latest_android_observed='v11.8.0-rc4.205'],['physical acceptance',d=>d['PITTI_CURRENT_STATE.json'].runtime.latest_device_evidence.acceptance='PASS'],['deployment id invented',d=>d['PITTI_CURRENT_STATE.json'].runtime.production_deployment.deployment_id='invented'],['PR179 authority',d=>d['PITTI_EXECUTION_LOCK.json'].authority.liveVerificationTargets.find(x=>x.pr===179).lane='REPAIR_AUTHORITY'],['PR178 missing',d=>d['PITTI_EXECUTION_LOCK.json'].authority.liveVerificationTargets=[]],['lock app',d=>d['PITTI_EXECUTION_LOCK.json'].runtime.appVersion='v11.8.0-rc4.204'],['device observed',d=>d['PITTI_EXECUTION_LOCK.json'].runtime.testChallengerAndroidObserved=true],['gate',d=>d['PITTI_COMMAND_CONTRACTS.json'].currentGate='DEPLOYED'],['seal',d=>d['PITTI_HANDOFF_SEAL.json'].status='SUPERSEDED_PENDING_RESEAL']];
for(const [n,f] of cases){const d=structuredClone(b);f(d);assert(validateAuthority(d).length,`must reject ${n}`)}
const h='a'.repeat(40),m='b'.repeat(40),ev={fresh:true,repo:'Muero42/draft-companion',canonicalBranch:'main',canonicalHead:m,head:h,clean:true,branch:'codex/rc4.205-postmerge-authority-reseal',prState:'OPEN',prHead:h,ciHead:h,authorizedWorkPackage:true,checks:['project_guardrails','release_contract_v2','candidate_package'].map(name=>({name,result:'PASS'}))};assert.deepEqual(validateContinuationEvidence(ev),[]);for(const f of [x=>x.fresh=false,x=>x.clean=false,x=>x.prHead=m,x=>x.ciHead=m,x=>x.authorizedWorkPackage=false,x=>x.checks.push({name:'project_guardrails',result:'PASS'})]){const x=structuredClone(ev);f(x);assert(validateContinuationEvidence(x).length)}console.log(`POSTMERGE_AUTHORITY_REGRESSION_PASS generation=v255 mutations=${cases.length} external=6`);
