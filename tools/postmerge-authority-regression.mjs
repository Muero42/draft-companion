import assert from 'node:assert/strict';import {loadAuthority,validateAuthority,validateContinuationEvidence} from './postmerge-authority-contract.mjs';
const b=loadAuthority();assert.deepEqual(validateAuthority(b),[],'baseline v256 authority must validate');const cases=[['generation',d=>d['PITTI_CURRENT_STATE.json'].handoff_generation='old'],['main',d=>d['PITTI_CURRENT_STATE.json'].authority.reconciled_main='bad'],['runtime lineage',d=>d['PITTI_CURRENT_STATE.json'].authority.rc4205_physical_acceptance.runtime_source_commit='bad'],['bounded pass erased',d=>d['PITTI_CURRENT_STATE.json'].authority.rc4205_physical_acceptance.bounded_device_acceptance_proven=false],['false full pass',d=>d['PITTI_CURRENT_STATE.json'].authority.rc4205_physical_acceptance.full_independent_evidence_acceptance_proven=true],['unavailable lane erased',d=>d['PITTI_CURRENT_STATE.json'].authority.rc4205_physical_acceptance.independent_unavailable_lanes=[]],['wrong records',d=>d['PITTI_CURRENT_STATE.json'].authority.rc4205_physical_acceptance.weekly_evidence.records=517],['timeout still blocking',d=>d['PITTI_CURRENT_STATE.json'].authority.rc4205_physical_acceptance.weekly_evidence.prior_sleeper_timeout_blocked_refresh=true],['semantics',d=>d['PITTI_CURRENT_STATE.json'].authority.rc4205_source_merge.repair_semantics='ros=false is correct'],['history',d=>d['PITTI_CURRENT_STATE.json'].authority.rc4204_physical_boundary.acceptance='PASS'],['lock physical',d=>d['PITTI_EXECUTION_LOCK.json'].runtime.androidVerified=false],['PR180',d=>d['PITTI_EXECUTION_LOCK.json'].authority.liveVerificationTargets=[]],['PR179',d=>d['PITTI_EXECUTION_LOCK.json'].authority.liveVerificationTargets.find(x=>x.pr===179).lane='REPAIR_AUTHORITY'],['gate',d=>d['PITTI_COMMAND_CONTRACTS.json'].currentGate='DONE'],['seal scope',d=>d['PITTI_HANDOFF_SEAL.json'].branch_locks.physical_acceptance_scope='FULL']];for(const[n,f]of cases){const d=structuredClone(b);f(d);assert(validateAuthority(d).length,`must reject ${n}`)}
const h='a'.repeat(40),m='b'.repeat(40),ev={fresh:true,repo:'Muero42/draft-companion',canonicalBranch:'main',canonicalHead:m,head:h,clean:true,branch:'codex/rc4.205-physical-pass-closeout',prState:'OPEN',prHead:h,ciHead:h,authorizedWorkPackage:true,checks:['project_guardrails','release_contract_v2','candidate_package'].map(name=>({name,result:'PASS'}))};assert.deepEqual(validateContinuationEvidence(ev),[]);for(const f of[x=>x.fresh=false,x=>x.clean=false,x=>x.prHead=m,x=>x.ciHead=m,x=>x.authorizedWorkPackage=false,x=>x.checks.push({name:'project_guardrails',result:'PASS'})]){const x=structuredClone(ev);f(x);assert(validateContinuationEvidence(x).length)}console.log(`POSTMERGE_AUTHORITY_REGRESSION_PASS generation=v256 mutations=${cases.length} external=6`);
