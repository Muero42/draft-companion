import assert from 'node:assert/strict';import {loadAuthority,validateAuthority,validateContinuationEvidence} from './postmerge-authority-contract.mjs';
const b=loadAuthority();assert.deepEqual(validateAuthority(b),[],'baseline v259 authority must validate');
const cases=[['generation',d=>d['PITTI_CURRENT_STATE.json'].handoff_generation='old'],['physical class',d=>d['PITTI_CURRENT_STATE.json'].authority.rc4205_pr186_physical_acceptance.classification='bad'],['qb mapping',d=>d['PITTI_CURRENT_STATE.json'].authority.rc4205_pr186_physical_acceptance.weekly_projections.positions.QB.mappedRows=79],['roster usable',d=>d['PITTI_CURRENT_STATE.json'].authority.rc4205_pr186_physical_acceptance.weekly_projections.active_roster.usable=12],['game type',d=>d['PITTI_CURRENT_STATE.json'].authority.rc4205_pr186_physical_acceptance.canonical_game_context.failureType='FETCH_EXCEPTION'],['upstream',d=>d['PITTI_CURRENT_STATE.json'].authority.rc4205_pr186_physical_acceptance.canonical_game_context.upstreamStatus=null],['pit panel claim',d=>d['PITTI_CURRENT_STATE.json'].authority.rc4205_pr186_physical_acceptance.expert_ranks.scope_note='PITTI panel proven'],['full acceptance',d=>d['PITTI_CURRENT_STATE.json'].authority.rc4205_pr186_physical_acceptance.full_independent_evidence_acceptance_proven=true],['runtime pending',d=>d['PITTI_CURRENT_STATE.json'].runtime.android_acceptance_pending=true],['command device',d=>d['PITTI_COMMAND_CONTRACTS.json'].currentBoundary.productionDeployment.deviceAcceptanceProven=false],['lock device',d=>d['PITTI_EXECUTION_LOCK.json'].runtime.testChallengerAndroidAccepted=false],['gate',d=>d['PITTI_COMMAND_CONTRACTS.json'].currentGate='DONE'],['seal gate',d=>d['PITTI_HANDOFF_SEAL.json'].exact_gate='DONE']];for(const[n,f]of cases){const d=structuredClone(b);f(d);assert(validateAuthority(d).length,`must reject ${n}`)}
const h='a'.repeat(40),m='b'.repeat(40),ev={fresh:true,repo:'Muero42/draft-companion',canonicalBranch:'main',canonicalHead:m,head:h,clean:true,branch:'pitti/v259-pr186-physical-acceptance',prState:'OPEN',prHead:h,ciHead:h,authorizedWorkPackage:true,checks:['project_guardrails','release_contract_v2','candidate_package'].map(name=>({name,result:'PASS'}))};assert.deepEqual(validateContinuationEvidence(ev),[]);for(const f of[x=>x.fresh=false,x=>x.clean=false,x=>x.prHead=m,x=>x.ciHead=m,x=>x.authorizedWorkPackage=false,x=>x.checks.push({name:'project_guardrails',result:'PASS'})]){const x=structuredClone(ev);f(x);assert(validateContinuationEvidence(x).length)}console.log(`POSTMERGE_AUTHORITY_REGRESSION_PASS generation=v259 mutations=${cases.length} external=6`);
