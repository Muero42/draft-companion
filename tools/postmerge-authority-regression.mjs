import assert from 'node:assert/strict';import {loadAuthority,validateAuthority,validateContinuationEvidence} from './postmerge-authority-contract.mjs';
const b=loadAuthority();assert.deepEqual(validateAuthority(b),[],'baseline v260 authority must validate');
const cases=[['generation',d=>d['PITTI_CURRENT_STATE.json'].handoff_generation='old'],['diag status',d=>d['PITTI_CURRENT_STATE.json'].authority.independent_evidence_lane_diagnosis.status='DONE'],['ranks',d=>d['PITTI_CURRENT_STATE.json'].authority.independent_evidence_lane_diagnosis.expert_ranks='UNAVAILABLE'],['panel',d=>d['PITTI_CURRENT_STATE.json'].authority.independent_evidence_lane_diagnosis.pitti_panel.status='AVAILABLE'],['game',d=>d['PITTI_CURRENT_STATE.json'].authority.independent_evidence_lane_diagnosis.canonical_game_context.status='AVAILABLE'],['team total',d=>d['PITTI_CURRENT_STATE.json'].authority.independent_evidence_lane_diagnosis.team_total.status='AVAILABLE'],['weather',d=>d['PITTI_CURRENT_STATE.json'].authority.independent_evidence_lane_diagnosis.weather_opponent.status='AVAILABLE'],['runtime',d=>d['PITTI_CURRENT_STATE.json'].runtime.independent_evidence_lane_status.pitti_panel='AVAILABLE'],['command',d=>d['PITTI_COMMAND_CONTRACTS.json'].currentBoundary.independentEvidenceLaneDiagnosis.status='DONE'],['lock',d=>d['PITTI_EXECUTION_LOCK.json'].authority.independentEvidenceLaneDiagnosis.status='DONE'],['gate',d=>d['PITTI_COMMAND_CONTRACTS.json'].currentGate='DONE'],['seal',d=>d['PITTI_HANDOFF_SEAL.json'].exact_gate='DONE']];for(const[n,f]of cases){const d=structuredClone(b);f(d);assert(validateAuthority(d).length,`must reject ${n}`)}
const h='a'.repeat(40),m='b'.repeat(40),ev={fresh:true,repo:'Muero42/draft-companion',canonicalBranch:'main',canonicalHead:m,head:h,clean:true,branch:'pitti/v260-independent-evidence-lane-diagnosis',prState:'OPEN',prHead:h,ciHead:h,authorizedWorkPackage:true,checks:['project_guardrails','release_contract_v2','candidate_package'].map(name=>({name,result:'PASS'}))};assert.deepEqual(validateContinuationEvidence(ev),[]);for(const f of[x=>x.fresh=false,x=>x.clean=false,x=>x.prHead=m,x=>x.ciHead=m,x=>x.authorizedWorkPackage=false,x=>x.checks.push({name:'project_guardrails',result:'PASS'})]){const x=structuredClone(ev);f(x);assert(validateContinuationEvidence(x).length)}console.log(`POSTMERGE_AUTHORITY_REGRESSION_PASS generation=v260 mutations=${cases.length} external=6`);
