import fs from 'node:fs';import assert from 'node:assert/strict';
const a=fs.readFileSync('app.js','utf8');
assert(a.includes("const APP_VERSION='v11.8.0-rc4.174'"),'rc4.174 version missing');
const ss=a.indexOf('function sanitizeResearchEvents('),se=a.indexOf('\nfunction loadResearchEvents',ss);
assert(ss>=0&&se>ss,'sanitizeResearchEvents missing');
const src=a.slice(ss,se).replace('function sanitizeResearchEvents','function');
const sanitize=new Function('return ('+src+')')();
assert.deepEqual(sanitize([null,1,'x',[],{}, {playerKey:'p'}]),[{}, {playerKey:'p'}]);
const early=a.indexOf('function bindCriticalSeasonControlsEarly()');
const fetchFresh=a.indexOf('\nasync function fetchDraftFresh',early);
const set=a.indexOf("setWorkspace(localStorage.getItem('v117_workspace')||'roster')");
const guarded=a.indexOf("try{updateResearchCacheStatus()}catch",set);
const lateVerify=a.indexOf('bindCriticalSeasonControlsEarly();',set);
const fresh=a.indexOf('renderSeasonLiveStateFreshness();',lateVerify);
assert(early>=0&&fetchFresh>early&&set>fetchFresh&&guarded>set&&lateVerify>guarded&&fresh>lateVerify,'critical Season early-control/startup sequence invalid');
console.log('SEASON_STARTUP_RESILIENCE_PASS rc4.174');
