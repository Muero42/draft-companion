# PITTI NEW CHAT BOOTSTRAP — v225
Generation: `20260902T0810Z-v225`

Read in order: PITTI_COMMAND_CONTRACTS.json → PITTI_CURRENT_STATE.json → PITTI_HANDOFF_SEAL.json → PITTI_EXECUTION_LOCK.json → PITTI_AUTO_PREFLIGHT.md → PITTI_PROJECT_STATE.md EOF → NEW_CHAT_HANDOFF_CURRENT.md → this file → HANDOFF_COMPLETENESS_MATRIX.md → actual main/PR/CI evidence.

Current truth: rc4.182 is physically installed and DEVICE_REJECTED. User reconfirmed Kader + experts do not load. rc4.169 remains accepted rollback authority. PR #98 latest head `de3eed64` is open/unmerged and latest Candidate Package, Project Guardrails, and Release Contract v2 all FAIL.

First executable gate: reconcile actual main + PR #98 latest head; fix exact latest-head guardrail/startup-sequence failures; then real-shape startup E2E must prove BOTH Kader hydration and expert initialization before any new device version.

AUTO/AUTO BLOCK: continuous same-turn execution; re-inventory after every package; no progress/status/ack messages, no empty replies, no false “AUTO läuft weiter”. External wait does not globally stop independent work. STATUS = report-only.

Do not let FA/Trade/expert-phase improvements overtake the startup defect. Post-fix priority: Kader → Experten → FA/Waiver → Trade/Boone → gradual in-season weighting.
