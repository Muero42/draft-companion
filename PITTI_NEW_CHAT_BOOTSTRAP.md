# PITTI NEW CHAT BOOTSTRAP — v194
Generation: `20260830T1359Z-v194`

Read/verify CURRENT + PASS SEAL + EXECUTION_LOCK + COMMAND_CONTRACTS + PROJECT_STATE EOF + HANDOFF + MATRIX once.

Continue only at:
`RC4.130_DEVICE_REFRESH_THEN_FULL_30_FIXTURE_V4V5_MOCK`

Immediate route:
1. Verify final main/gh-pages parity and source authority rc4.130.
2. One controlled device refresh; verify visible rc4.130.
3. Confirm v4/v5 operational.
4. Start a **fresh** complete 15-round realistic mock, analyzing BOTH v4 and v5 before every own pick.
5. Export backup; require exactly **30 current-draft decisionFixtures = 15 paired states × 2 profiles** before any model comparison.
6. If 30/30 PASS, analyze v4 vs v5 and Return calibration. If not, diagnose evidence retention first.

Authority:
- rc4.129 fail-closed storage error root cause = `history.slice(-0) === history.slice(0)`, so intended history-free quota retry retained history.
- rc4.130 explicitly uses [] for zero-history recovery and compacts redundant rankedPool evidence while preserving active-fixture atomicity.
- latest backup 13-40-34-982Z = only current picks 9/12 paired (4 fixtures) plus 22 historical fixtures; not a full test.
- v4 PRIMARY; v5 CHALLENGER; v3 failsafe. No weight/source retune.
- no phone trial-and-error, cache clear, or reinstall.
- STATUS no-tools/report-only; AUTO BLOCK silent execution.
