# PITTI NEW CHAT BOOTSTRAP — v192
Generation: `20260830T1010Z-v192`

Read/verify CURRENT + PASS SEAL + EXECUTION_LOCK + COMMAND_CONTRACTS + PROJECT_STATE EOF + HANDOFF + MATRIX once.

Continue only at:
`RC4.117_DEVICE_V4_ACCEPTANCE`

Immediate route:
1. rc4.117 source/package off-device PASS and gh-pages/main byte parity PASS are already proven.
2. Request one device refresh/install.
3. Verify rc4.117 and test v4 first.
4. Move immediately to realistic mocks if v4 PASS.
5. v5 only if time remains; never let v5 block a validated v4 draft build.

Authority:
- v3 frozen failsafe/control.
- v4 individual-only, native Koerner, exact weighted blueprint; primary target.
- v5 = v3 + Koerner funded from Draft Sharks first; optional secondary test.
- positional aggregation and Overall provenance are separate.
- no phone trial-and-error or duplicate source research.
- STATUS no-tools/report-only.
- AUTO BLOCK silent; empty replies forbidden.
